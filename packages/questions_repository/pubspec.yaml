name: questions_repository

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^3.4.1
  rxdart: ^0.26.0
  equatable: ^2.0.0
  firebase_core: ^1.20.0
